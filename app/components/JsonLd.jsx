// Renderar JSON-LD strukturerad data. data kan vara ett objekt eller en array.
export default function JsonLd({ data }) {
  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }}
    />
  );
}
